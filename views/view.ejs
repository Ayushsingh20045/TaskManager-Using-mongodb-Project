<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Task</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        /* Global Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%;
            width: 100%;
            font-family: 'Poppins', sans-serif;
        }

        /* Body Styling */
        body {
            background: linear-gradient(135deg, #0d0d0d, #1a1a1a);
            color: #f5f5f5;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Navbar Styling */
        .navbar {
            background: linear-gradient(90deg, #202020, #151515);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.6);
            padding: 15px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            width: 100%;
            position: fixed;
            top: 0;
            z-index: 1000;
        }

        .navbar a {
            color: #f5f5f5;
            text-decoration: none;
            margin: 0 15px;
            font-size: 18px;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        .navbar a:hover {
            color: #50c878;
        }

        .container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background: #202020;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.6);
        }

        h1 {
            text-align: center;
            font-size: 2em;
            margin-bottom: 20px;
            color: #50c878;
        }

        .task-details {
            margin-top: 20px;
        }

        .task-details p {
            font-size: 1.1em;
            line-height: 1.6;
        }

        .task-details span {
            font-weight: bold;
            color: #50c878;
        }

        .back-btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background: #50c878;
            color: #0d0d0d;
            text-decoration: none;
            font-weight: bold;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .back-btn:hover {
            background: #45a049;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <div class="navbar">
        <div class="left">
            <a href="/home">Home</a>
        </div>
        <div class="right">
            <a href="/create">Create New Task</a>
        </div>
    </div>

    <!-- Main View Task Container -->
    <div class="container">
        <h1>Task Details</h1>
        <div class="task-details">
            <p><span>Title:</span> <%= view.title %></p>
            <p><span>Description:</span> <%= view.description %></p>
            <p><span>Created At:</span> <%= new Date(view.createdAt).toDateString() %></p>
            <p><span>Status:</span> <%= view.status ? 'Completed' : 'Pending' %></p>
        </div>
        <a href="/home" class="back-btn">Back to Home</a>
    </div>
</body>
</html>

